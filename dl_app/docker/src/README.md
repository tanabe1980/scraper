# docker/src

ローカルDocker実行のためのオーバーライド用ソースを配置する。

- 基幹ロジックは `dl_app/` 直下の実装を利用する。
- 本ディレクトリでは「Docker時だけ差し替えたい設定・薄いラッパー」のみを管理する。
- 共通処理はここに重複実装しない。

サブディレクトリ:
- `overrides/`: Docker時に上書きする薄い実装
- `bootstrap/`: Docker起動時の初期化スクリプト
